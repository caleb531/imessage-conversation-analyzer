/**
 * Shared chart palette expressed as CSS variable references.
 *
 * Keeping these values in one place ensures every chart component and route
 * uses the same ordered color scale.
 */
export const chartColors: string[] = [
    'var(--color-chart-1)',
    'var(--color-chart-2)',
    'var(--color-chart-3)',
    'var(--color-chart-4)',
    'var(--color-chart-5)',
    'var(--color-chart-6)',
    'var(--color-chart-7)'
];
